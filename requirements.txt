# ============================================================================
#  Institutional Valuation System — Python dependencies
#  Install:  pip install -r requirements.txt
# ============================================================================
#
#  CRITICAL NOTICE ABOUT YFINANCE
#  -------------------------------
#  yfinance < 1.0 breaks ALL price downloads SILENTLY: it doesn't raise an
#  exception, it just returns empty. The effect on the application is that
#  prices freeze at the purchase cost and the portfolio shows a false +6%.
#  This file used to pin `yfinance==0.2.40`, which is exactly the affected
#  version. Do NOT go below 1.0.
#
#  Diagnose in seconds:  GET /api/health  returns the interpreter path and
#  the yfinance version the backend is actually using.
# ============================================================================

# ── API and server ──────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
python-multipart>=0.0.9

# ── Market data ──────────────────────────────────────────────────────────────
yfinance>=1.2,<2.0        # see header notice: NEVER < 1.0
requests>=2.32
curl_cffi>=0.7            # HTTP session used internally by yfinance

# ── Numerical computation ───────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
scipy>=1.11               # Monte Carlo: Kolmogorov-Smirnov test

# ── Reports ──────────────────────────────────────────────────────────────────
reportlab>=4.2            # PDF generation for investment theses

# ── Data science: notebooks 01–04 ───────────────────────────────────────────
scikit-learn>=1.4         # models, metrics, validation
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
nbformat>=5.10

# ── Gen AI: notebook 05 ──────────────────────────────────────────────────────
# Retrieval (chunking + TF-IDF + cosine) works with scikit-learn alone,
# already listed above: the notebook runs end-to-end WITHOUT an API key.
# `anthropic` is only needed for the last cell, the actual LLM call,
# which also requires the ANTHROPIC_API_KEY environment variable.
anthropic>=0.40             # optional — the cell is guarded with try/ImportError

# ── Presentation generation: presentacion/build_deck.py ─────────────────────
# Only needed if IVS_Presentacion_TFM.pptx has to be regenerated.
python-pptx>=1.0            # optional
Pygments>=2.17              # optional — syntax-highlighted code screenshots
Pillow>=10.0                # optional — rasterizing those screenshots
